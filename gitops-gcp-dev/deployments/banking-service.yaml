apiVersion: v1
kind: Service
metadata:
  name: banking-service
  namespace: banking-dev
  labels:
    app: banking-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: banking-service
